# feel_capture (필캡쳐) — QA.md

이 util 작업 중 발견된 버그, 이상 동작, 검증 실패 사례를 기록한다.

---

## 2026-07-23 — (개발 중 자체 발견) 스모크 테스트에서 녹화 완료 시그널이 수신되지 않는 것처럼 보임

- **증상**: `RecorderThread`를 실제로 `.start()`시켜 gif 녹화 후 `.wait()`로 스레드 종료를 기다렸는데, 연결해둔 `finished_result` 콜백이 호출되지 않은 것처럼 보임(`results` 딕셔너리가 비어 있음).
- **원인**: 코드 버그가 아니라 테스트 스크립트의 문제였음. `RecorderThread`(QThread)가 별도 OS 스레드에서 `finished_result.emit(...)`을 호출하면 Qt가 이를 메인 스레드의 이벤트 큐에 넣는 큐드 커넥션으로 처리하는데, 테스트 스크립트가 `QApplication.exec_()`나 `processEvents()`를 호출하지 않아 큐에 쌓인 시그널이 실제로 전달되지 않았던 것. 실제 앱(`main.py`)은 `app.exec_()`로 이벤트 루프가 항상 돌고 있으므로 이 문제와 무관함.
- **해결**: 테스트 스크립트에 `app.processEvents()` 호출을 추가해 큐에 쌓인 시그널을 처리하도록 수정 → gif(2901 bytes)/mp4(5089 bytes) 파일이 실제로 정상 생성되고 `finished_result`가 `(True, "저장됨: ...")`로 정확히 전달됨을 확인.
- **비고**: 별도의 순수 `threading.Thread`로 `RecorderThread.run()`을 직접 호출해본 결과, 이벤트 루프 유무와 무관하게 실제 녹화(파일 쓰기)는 항상 정상 동작함을 재확인함 — 즉 문제는 "시그널 전달 확인 방법"에만 있었고 녹화 로직 자체는 처음부터 정상이었음.

## 검증 요약 (2026-07-23, 초기 작성 — 헤드리스 스모크 테스트 + 실제 exe 빌드/실행)

개발 중 직접 실행하여 검증 (별도 서브에이전트 독립 검증은 아래 "예정" 항목 참고).

- **모듈 임포트/문법**: `py_compile`로 전체 모듈 컴파일 확인, `QT_QPA_PLATFORM=offscreen` 헤드리스 환경에서 모든 모듈 import 성공.
- **config 로드/저장 라운드트립**: 임시 `%APPDATA%` 경로로 `save_config`→`load_config` 실행, 값 일치 확인.
- **화면 캡처**: `grab_virtual_desktop()`이 실제 화면 크기(1920x1080)를 정상 반환, `grab_region()`으로 부분 영역 캡처 확인.
- **리사이즈**: 비율(%) 모드로 100x80 → 50x40 축소가 정확히 계산됨을 확인.
- **정지 이미지 저장**: 파일 저장 경로로 실제 png 파일이 생성됨을 확인 (파일 존재 + 개수 확인).
- **오버레이/위젯 생성**: `DragSelectOverlay`, `RegionBox`, `RecordIndicator`, `SettingsDialog`를 헤드리스 환경에서 실제로 생성해 예외 없이 동작함을 확인 (`RegionBox.rect_in_screen()`, `SettingsDialog.result_config()` 등 핵심 메서드 호출 포함).
- **실제 녹화(gif/mp4)**: `RecorderThread`를 실제로 1.2초간 구동 후 정지 → gif(2901 bytes)/mp4(5089 bytes) 파일이 실제로 생성되고 0바이트가 아님을 확인.
- **TrayApp 전체 흐름**: `QSystemTrayIcon.isSystemTrayAvailable`을 강제로 `True`로 만들어 `TrayApp`을 실제로 생성 → 영역 모드에서 `on_hotkey()` 호출 시 즉시 정지 이미지가 파일로 저장됨을 확인, 드래그 모드에서 `on_hotkey()` 호출 시 `DragSelectOverlay`가 실제로 열리고 ESC(취소 시뮬레이션)로 정상적으로 닫히며 참조가 정리됨을 확인.
- **PyInstaller exe 빌드**: `build_exe.ps1`로 실제 빌드 성공 (`dist\FeelCapture.exe` 생성 확인). 빌드된 exe를 실제로 실행해 4초간 크래시 없이 유지됨을 확인 후 프로세스 종료. `Hidden import "sip" not found!` 경고가 뜨지만 PyQt5 exe 빌드에서 흔히 나타나는 무해한 경고로, 실제 실행에는 영향이 없었음(exe가 정상 기동).
- **아이콘**: `generate_icons.py`로 다중 해상도(.ico) 아이콘이 정상 생성됨을 확인.

### 검증 예정 (사용자 승인 시)
- 서브에이전트를 통한 독립 검증(코드 미수정, 실행/테스트만 수행)을 별도로 진행 예정 — CLAUDE.md 워크플로우에 따름.
- 실제 데스크톱 환경에서: 단축키를 실제로 눌러 드래그 캡쳐/영역 캡쳐/녹화 시작·종료가 사람 조작으로도 정상 동작하는지, 클립보드 붙여넣기가 실제 다른 앱(그림판 등)에서 정상 확인되는지, 영역 박스의 마우스 리사이즈/이동/우클릭 프리셋 메뉴가 실제 마우스 조작으로 자연스러운지는 headless 자동화로는 완전히 검증되지 않으므로 사람이 직접 실행해보는 것을 권장.

---

## 2026-07-23 — (서브에이전트 독립 검증에서 발견) `overlay_region.RegionBox` 리사이즈 시 서/북 핸들을 반대쪽 경계 너머로 드래그하면 박스가 순간이동함

- **증상**: 영역 박스의 서(w)/북(n)/북서(nw) 리사이즈 핸들을 마우스로 반대쪽 경계보다 더 멀리(빠르게) 드래그하면, 박스가 그 자리에서 최소 크기로 자연스럽게 멈추는 게 아니라 **커서 위치로 통째로 순간이동(teleport)하면서 반대쪽(원래 고정되어 있어야 할) 경계 위치를 완전히 잃어버린다.**
  - 재현(코드 레벨, `overlay_region.RegionBox` 단위 테스트로 확인): 화면 좌표 `x=100,y=100,w=200,h=150`(우측 경계 x=300)인 박스에서 서쪽 경계를 잡고 커서를 `x=500`까지 오른쪽으로 드래그하면, 결과 geometry가 `x=498, width=30`이 된다. 즉 원래 오른쪽 경계(300)는 완전히 사라지고, 박스가 커서 바로 아래에 30px짜리 상자로 다시 생성된 것처럼 보인다.
  - 반대로 동(e)/남(s)/남동(se) 핸들을 반대쪽 경계 너머로 드래그하는 경우는 이 문제가 없다 — 원래 고정된 좌측/상단 모서리에 정상적으로 앵커링된 채 최소 크기(30px)로 클램프된다(예: se 핸들을 nw 모서리 너머로 드래그 → `x=100,y=100,w=30,h=30`으로 정상적으로 원래 좌상단에 앵커됨).
- **원인**: `overlay_region.py`의 `mouseMoveEvent`에서 리사이즈 시 `geom.setLeft()`/`setTop()`(서/북 핸들)로 좌측·상단 좌표를 직접 이동시키는데, 이때 반대쪽(우측/하단) 좌표는 `self._drag_start_geom`의 원래 값 그대로 둔다. 드래그가 반대쪽 경계를 넘어서면 `width()`/`height()`가 음수가 되고, 그 다음 클램프 코드(`if geom.width() < MIN_SIZE: geom.setWidth(MIN_SIZE)`)가 실행된다. `QRect.setWidth()`는 `x()`(좌측 좌표)는 그대로 두고 우측 좌표만 재계산하는데, 서/북 핸들의 경우 바로 그 `x()`/`y()`가 **방금 드래그로 움직인(그래서 반대쪽 경계를 이미 넘어버린) 값**이라서, 클램프가 "원래 고정되어 있어야 할 반대쪽 경계"가 아니라 "방금 넘어간 커서 위치"를 앵커로 삼아버린다. 동/남/se 핸들은 애초에 `x()`/`y()`(좌측/상단)를 건드리지 않으므로 이 문제가 발생하지 않는다 — 즉 버그는 서/북 계열 핸들(w, n, nw, 그리고 ne/sw는 각각 한 축만)에 국한된 비대칭 문제다.
- **해결 (2026-07-23)**: `mouseMoveEvent`를 서브에이전트가 제안한 방식 그대로 수정 — `setWidth()`/`setHeight()`로 사후 클램프하는 대신, w/e/n/s 각 핸들마다 "반대쪽(고정되어야 할) 경계"를 앵커로 두고 `min()`/`max()`로 새 좌표 자체를 `앵커 ± (MIN_SIZE - 1)` 이내로 미리 제한한 뒤 `setLeft()`/`setRight()`/`setTop()`/`setBottom()`을 호출하도록 변경. 재현 스크립트(`x=100,y=100,w=200,h=150` 박스의 서쪽 핸들을 x=500까지 드래그)로 재검증 → 결과가 `x=270,w=30`(오른쪽 경계 299 그대로 앵커됨)으로 정상화됨을 확인.
- **검증 방법**: 실제 마우스 조작이 아니라, `unittest.mock.Mock`으로 `QMouseEvent`를 흉내내어 `RegionBox.mousePressEvent`/`mouseMoveEvent`를 직접 호출하는 방식으로 재현(headless, `QT_QPA_PLATFORM=offscreen`). 실제 사람이 마우스로 빠르게 드래그했을 때 시각적으로 어떻게 보이는지(순간이동이 눈에 띄게 거슬리는 정도)는 이번 검증에서 실제 마우스 조작으로 확인하지 못했음 — 코드 레벨의 좌표 계산 버그만 확인함.

## 2026-07-23 — (서브에이전트 독립 검증에서 발견) `config.load_config()`가 `region_presets`이 빈 리스트인 config.json을 만나면 기본 프리셋 3개로 복구되지 않고 빈 리스트를 그대로 사용함

- **증상**: `%APPDATA%\FeelCapture\config.json`에 `"region_presets": []`가 들어있는 상태로 `load_config()`를 호출하면, 결과 `cfg["region_presets"]`가 (의도된 것으로 보이는) 기본 3개 프리셋으로 복구되지 않고 그대로 빈 리스트가 된다. 이 상태에서 영역 박스 우클릭 메뉴의 "프리셋 크기" 서브메뉴는 항목이 하나도 없는 빈 메뉴가 된다(크래시는 아니지만 사용자가 프리셋을 전혀 쓸 수 없게 됨).
- **원인**: `config.py`의 `load_config()`에서 `cfg.update(data)`가 먼저 실행되어 파일에 있는 `region_presets`(빈 리스트 포함) 값으로 `cfg`를 무조건 덮어쓴다. 그 다음에 있는 방어 코드
  ```python
  if "region_presets" in data and isinstance(data["region_presets"], list) and data["region_presets"]:
      cfg["region_presets"] = data["region_presets"]
  ```
  는 "`data["region_presets"]`가 falsy(빈 리스트)면 기본값으로 되돌린다"는 의도로 보이지만, 실제로는 이미 `cfg.update(data)`가 `cfg["region_presets"]`를 빈 리스트로 덮어쓴 뒤이므로, 이 `if`문의 조건이 거짓이 되어 그냥 통과할 뿐 아무것도 복구하지 않는다. 결과적으로 "빈 리스트면 기본값 유지"라는 의도와 반대로 빈 리스트가 그대로 남는다.
  (참고: `region_box`도 같은 `cfg.update(data)` → 부분 병합 패턴을 쓰지만, `region_box`는 항상 `{**DEFAULT_CONFIG["region_box"], **data["region_box"]}`로 병합하므로 이 문제가 없음을 별도로 확인함 — `region_presets`만 "덮어쓴 뒤 조건부로 되돌리려는" 방식이라 이 문제가 있음.)
- **영향 범위**: 실제 UI(`settings_dialog.SettingsDialog.result_config()`)는 프리셋 3칸을 항상 채워서 반환하므로(이름이 비어도 "프리셋N"으로 대체됨) 정상적인 앱 사용 흐름에서는 절대 빈 리스트가 저장되지 않는다. 즉 이 버그는 `config.json`을 수동으로 편집하거나 다른 도구로 잘못된 값을 넣었을 때만 발생하는 낮은 심각도의 엣지 케이스임.
- **해결 (2026-07-23)**: 서브에이전트가 제안한 방식 그대로 수정 — `cfg.update(data)` 이후 `if not cfg.get("region_presets"): cfg["region_presets"] = <기본값 3개>`로 무조건 사후 검사하도록 변경(빈 리스트/누락 모두 커버). `region_presets: []`만 담긴 config.json으로 재검증 → 기본 프리셋 3개(1920x1080/1280x720/800x600)로 정상 복구됨을 확인.
- **검증 방법**: 격리된 임시 `%APPDATA%` 경로에 `{"region_presets": []}`만 담긴 `config.json`을 직접 작성한 뒤 `load_config()` 호출 → 결과가 빈 리스트임을 확인(headless, 재현 스크립트로 검증).

---

## 검증 요약 (2026-07-23, 서브에이전트 독립 검증)

CLAUDE.md 워크플로우에 따라 별도 에이전트가 위 개발자 자체 검증과 무관하게 소스 7개 파일을 처음부터 다시 읽고, 실제로 코드를 실행/조작하여 독립적으로 검증함. 아래는 실제로 수행하고 확인한 항목과 수치.

**결론: 전반적으로 정상 동작. 코드 로직 버그 2건 발견(위 항목, 둘 다 낮은/중간 심각도, 크래시 없음) — QA.md에 기록함. 그 외 개발자의 기존 "검증 요약" 항목들은 모두 재확인되었고 부정확한 내용은 없었음.**

- **모듈 컴파일**: `py_compile`로 7개 소스 파일 전체 재컴파일 성공.
- **`config.py`**: 격리된 임시 `%APPDATA%`에서 (1) 파일 없을 때 기본값, (2) 문법이 깨진 JSON → 전체 기본값 폴백, (3) 일부 키만 있는 부분 config → 나머지는 기본값 유지하며 병합, (4) `region_box`에 x/y만 있는 부분 병합 → w/h/locked는 기본값 유지, (5) `region_presets: []`인 경우 → **버그 발견(위 항목)**, (6) `extension`에 정수형처럼 잘못된 타입이 들어와도 예외 없이 그대로 통과됨(런타임에 문자열 비교/포매팅에만 쓰이므로 크래시로 이어지지는 않음, 참고용으로 기록), (7) `save_config`→`load_config` 라운드트립 정상, 모두 스크립트로 실행 확인.
- **`capture_core.py`**: 실제 화면에서 `grab_virtual_desktop()`으로 1920x1080 실제 픽셀 캡처 확인(픽셀 값 실측), `grab_region()`으로 (10,10)-(210,160) 부분 영역을 잘라 가상 데스크톱 크롭과 100개 샘플 픽셀을 대조해 좌표 정합성 확인(정적 화면 영역이라 100/100 일치). `apply_resize`: 비율(%) 50%(100x80→50x40), 고정 크기 중 폭만 지정(높이 None → 비율 유지 50x40), 높이만 지정(폭 None → 비율 유지 50x40), 폭/높이 둘 다 0 → resize_enabled=True여도 리사이즈 없이 원본 유지(사양대로의 동작이나 "리사이즈 켬"인데 아무 일도 안 일어나는 엣지케이스이므로 참고로 기록, 버그 아님), 강제 스트레치(30x30), resize_enabled=False → 원본 객체 그대로 반환, 모두 기대값과 일치. 파일명 충돌: 같은 폴더에 같은 타임스탬프로 3번 연속 저장 시도 → `_1`, `_2` 접미사로 3개 파일 모두 겹치지 않고 저장됨을 확인.
- **`overlay_region.py`**: `RegionBox` 리사이즈 엣지/코너 히트테스트 및 드래그 수학을 `Mock` 기반 `QMouseEvent`로 직접 조작해 검증 → **버그 발견(위 항목)**.
- **`recorder.py`**: `RecorderThread`를 gif/mp4/avi 각각 실제로 ~1.5초(fps=10) 구동 후 정지. 결과: gif 4525 bytes(총 재생시간 1500ms, PIL로 프레임 duration 합산해 확인 — 참고: 캡처 영역이 정적이라 Pillow gif 인코더가 동일한 연속 프레임을 자동으로 1프레임+긴 duration으로 병합하는 것을 별도 합성 테스트로 확인함, 이는 feel_capture의 버그가 아니라 Pillow의 정상적인 gif 인코딩 동작이며 총 재생시간은 정확히 보존됨), mp4 14004 bytes(cv2.VideoCapture로 프레임 수 16, 해상도 320x240, 첫 프레임 디코드 성공 확인), avi 19228 bytes(cv2.VideoCapture로 프레임 수 16, 해상도 320x240, 첫 프레임 디코드 성공 확인). 세 포맷 모두 `finished_result` 시그널이 `(True, "저장됨: ...")`로 정상 전달됨을 확인.
- **`main.py`(TrayApp) 전체 흐름**: `QSystemTrayIcon.isSystemTrayAvailable`을 `True`로 몽키패치해 헤드리스로 `TrayApp`을 실제 생성, `on_hotkey()`를 아래 4가지 조합 전부에서 실행해 확인:
  1. 영역+정지포맷(png): 즉시 1장 저장됨.
  2. 영역+녹화포맷(gif): 첫 호출로 녹화 시작(인디케이터 표시 확인), 두번째 호출로 정지·저장됨. 녹화 중 `on_hotkey()`를 다시 호출하면 새 캡쳐/오버레이를 시도하지 않고 정지만 함을 확인.
  3. 드래그+정지포맷(png): 오버레이가 실제로 열림. 오버레이가 열린 상태에서 `on_hotkey()`를 재호출해도 두번째 오버레이가 열리지 않음을 확인(재진입 방지 정상). ESC로 취소 시 오버레이 참조가 정리됨을 확인. 이후 실제 `QMouseEvent`(press/move/release)로 드래그 선택을 시뮬레이션해 정상적으로 1장 저장됨을 확인.
  4. 드래그+녹화포맷(mp4): 드래그로 영역을 지정하면 그 즉시 그 영역에 대한 녹화가 시작됨을 확인(`recorder.x/y/w/h`가 드래그 사각형과 일치), 녹화 중 `on_hotkey()` 재호출 시 새 드래그 오버레이를 열지 않고 정지·저장만 함을 확인(재진입 방지 정상), `finished_result`로 전달된 실제 mp4 파일 경로가 존재하고 0바이트가 아님을 확인.
  (참고: mp4/avi는 `make_output_path()`가 녹화 **시작 시점**에 호출되고 `cv2.VideoWriter`가 첫 프레임 기록 시 즉시 파일을 생성하므로, "정지 전/후" 파일 목록을 단순 diff하면 이미 존재하는 파일이라 false negative가 남 — 이는 검증 스크립트 작성 과정에서 발견한 테스트 방법론상의 함정이었을 뿐 실제 버그는 아니었음. gif는 반대로 종료 시점에 한 번에 파일이 생성되므로 diff 방식이 유효함.)
- **`settings_dialog.py`**: 확장자를 gif로 바꾸면 클립보드 라디오가 자동으로 "파일" 대상으로 전환되고 클립보드 라디오 자체가 비활성화됨을 확인, 정지 포맷으로 되돌리면 다시 활성화됨을 확인(단, 자동으로 다시 체크되지는 않음 — 사양과 일치). `result_config()`로 모드/저장방식/폴더/확장자/fps/리사이즈(비율 75%)/단축키(`ctrl+alt+p`)/프리셋 3개(한글 이름 포함) 전체가 정확히 왕복됨을 확인. 고정 리사이즈에서 폭을 0(비움)으로 두면 `resize_width`가 `None`으로 저장됨(비율 유지 사양과 일치)도 확인. 저장방식=파일이면서 출력폴더가 공백(공백 문자만 입력해도 포함)일 때 `QMessageBox.warning` 경로로 막히고 다이얼로그가 accept되지 않음을 확인, 단축키를 비워도 마찬가지로 막힘을 확인. 반대로 저장방식=클립보드면 출력폴더가 비어 있어도 정상적으로 accept됨을 확인(클립보드일 때 폴더 검증을 건너뛰는 것이 사양과 일치).
- **`main.py`(HotkeyManager)**: `set_hotkey()`로 등록 시 예외 없음, `keyboard`가 내부적으로 관리하는 `_hotkeys` 등록 항목 수가 동일 단축키 재등록/단축키 교체 시에도 늘어나지 않음(중복 바인딩 없음)을 확인, `clear()` 후 등록 항목이 0이 됨과 재호출해도 예외 없음을 확인, 빈 문자열/문법이 잘못된 단축키 문자열을 넣어도 예외가 새어나가지 않고 내부적으로 캐치되어 로그만 출력됨을 확인. **단, "실제로 키를 눌러서 콜백이 진짜로 발동하는지"는 이번 검증 환경에서 확인하지 못함** — `keyboard.send()`/`press_and_release()`로 합성 키 입력을 보내고 `keyboard.hook()`으로 직접 수신을 시도했으나, 어떤 키 조합에 대해서도 훅이 이벤트를 단 한 건도 수신하지 못함(세션은 콘솔 세션 1/Active로 확인되어 헤드리스 문제는 아니며, 이 실행 환경 자체의 제약으로 보임 — processing.md에 이미 기록된 "일부 환경에서 `keyboard`의 전역 후킹이 차단될 수 있다"는 제약사항과 부합). 따라서 등록/해제 로직 자체는 검증했으나, 실제 물리적 키 입력 → 콜백 발동의 종단간(End-to-End) 확인은 이번에도 하지 못했음(사람이 직접 실행 환경에서 눌러보는 방식의 확인이 필요).
- **실제 GUI 스모크 실행**: 격리된 임시 `%APPDATA%`로 `main.py`를 오프스크린 없이 실제로 실행 → 4초간 크래시 없이 유지, stderr 출력 없음을 확인. 전체 화면 스크린샷으로 트레이 아이콘 존재를 시각적으로 확인 시도했으나, 트레이 오버플로(`^`) 메뉴 클릭이 좌표상 정확히 맞아떨어지지 않아 트레이 아이콘 자체를 스크린샷으로 명확히 식별하지는 못함 — **이 부분(트레이 아이콘이 실제로 눈에 보이는지)은 이번 검증에서 시각적으로 확정하지 못했음**, 다만 `main()` 실행 중 `QSystemTrayIcon.isSystemTrayAvailable()`이 실패했다면 즉시 `QMessageBox.critical` 후 `sys.exit(1)`로 종료되었을 것이므로(코드 확인됨), 프로세스가 4초간 정상 유지되었다는 것 자체가 트레이 아이콘 생성 경로가 예외 없이 통과했음을 강하게 시사함. 실제 단축키(`ctrl+shift+s`)를 눌러 스크린샷이 저장되는지는 위 `keyboard` 훅 미수신 문제로 이번 환경에서 확인하지 못함.
- **PyInstaller exe 재빌드/실행**: `build_exe.ps1`을 다시 실행해 빌드 성공(`dist\FeelCapture.exe` 재생성) 확인. 격리된 `%APPDATA%`로 exe를 실제 실행 → PyInstaller onefile 특성상 부모/자식 2개 프로세스(PID 예: 9068, 24568)로 뜨는 것을 확인(정상적인 onefile 부트로더 동작), 둘 다 6초 이상 `Responding=True` 상태로 크래시 없이 유지됨을 확인 후 정상 종료.
- **프로세스 정리 관련 참고사항(버그는 아니나 기록)**: 검증 시작 시점에 이미 `E:\46 util\utils\feel_capture\dist\FeelCapture.exe` 프로세스(PID 26968)가 15:12경부터 떠 있는 것을 발견함 — 개발자의 QA.md 기존 기록("4초간 유지 확인 후 프로세스 종료")과 달리 실제로는 종료되지 않고 15분 이상 남아 있었던 것으로 보임. 이번 검증에서 발견 즉시 종료 처리함. 코드 버그는 아니고 이전 검증 세션의 정리 누락으로 판단됨.
- **정리**: 이번 검증에서 사용한 모든 격리 테스트는 스크래치패드 하위의 임시 `%APPDATA%` 경로만 사용했으며, 실제 사용자 `%APPDATA%\FeelCapture\config.json`은 검증 종료 시점에 존재하지 않음(`Test-Path` 확인)을 재확인함 — 이번 검증으로 실제 사용자 설정 파일은 생성/변경되지 않았음. 검증 중 띄운 모든 프로세스(테스트용 `main.py` 실행, 재빌드된 exe 실행, 그리고 위에서 발견한 기존 leftover exe)는 모두 종료 처리했으며, 검증 종료 후 `python.exe`/`FeelCapture.exe` 프로세스가 남아있지 않음을 최종 확인함.
